export class Booking {
    // tslint:disable-next-line: max-line-length
    constructor(public id: string, public placeId: string, public userId: string, public placeTitle: string, public placeImage: string, public guestNo: number, public firstName: string, public lastName: string, public dateFrom: Date, public dateTo: Date) {}
}
